export const createFilmsListExtraTemplate = (extraTitle) => (
  `<section class="films-list films-list--extra">
    <h2 class="films-list__title">${extraTitle}</h2>
    <div class="films-list__container"></div>
  </section>`
);
